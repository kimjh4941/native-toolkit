<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Android 13以降で必要 -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />

    <!--
        この CallStyle 実装は実際の通話アプリではないため、確認用途として
        FOREGROUND_SERVICE_SPECIAL_USE を使っています。

        将来、本物の通話アプリとして phoneCall type の FGS を使う場合は、例えば以下を検討します。
        <uses-permission android:name="android.permission.FOREGROUND_SERVICE_PHONE_CALL" />

        Android 14/15 系では上記に加えて、MANAGE_OWN_CALLS または default dialer role など、
        実通話アプリとしての条件を満たしていないと phoneCall type FGS は拒否されます。
    -->

    <application>
        <receiver
            android:name=".notification.data.repository.ScheduledNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".notification.data.repository.ScheduledNotificationBootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!--
            現状の service はサンプル用途のため specialUse を使っています。
            将来 phoneCall に切り替える場合は、android:foregroundServiceType="phoneCall" へ変更し、
            さらに実通話アプリとしての条件（MANAGE_OWN_CALLS / default dialer role など）を満たす必要があります。
        -->
        <service
            android:name=".notification.presentation.call.CallStyleForegroundService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="call_style_notification" />
        </service>
    </application>
</manifest>