# 検査2「サンプルアプリ整合」で意図的に許容する文字列リテラル・引数ラベル。
#
# 1 行 1 エントリ。`#` 以降は行コメント。
# クォート付き（"Hello"）でもクォートなし（Hello）でも一致します。
# 引数ラベルはコロンまで書きます（例: displayMode:）。
#
# ここに足す前に、本当にサンプルアプリと乖離させてよいかを確認してください。
# 「利用者が自分の値に置き換える前提の説明用の値」だけが対象です。
# 実際に動く操作の ID・パラメータ値は、必ずサンプルアプリと一致させます。

# API の呼び出し形だけを示す導入例。share.md も同じ値を使っています。
Hello
