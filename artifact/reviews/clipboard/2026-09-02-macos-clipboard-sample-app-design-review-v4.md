# レビュー結果（範囲を絞ったレビュー）

- 日付: 2026-09-02
- 対象ファイル: `artifact/designs/clipboard/2026-09-02-macos-clipboard-sample-app-design-v4.md`
- 前版: `artifact/designs/clipboard/2026-09-02-macos-clipboard-sample-app-design-v3.md`
- 前回レビュー: `artifact/reviews/clipboard/2026-09-02-macos-clipboard-sample-app-design-review-v3.md`（高 2 / 中 8 / 低 10 = 20 件）
- 併せて確認したライブラリ側修正: `mac/MacLibrary/MacLibraryTests/Clipboard/ClipboardLogAuditTests.swift`、`scripts/check_design_consistency.py`
- レビュー範囲: 依頼どおり (1) 前回指摘 20 件の解消状況、(2) 着手をブロックするものの有無、(3) ライブラリ側の監査修正の実効性、の 3 点のみ。v3 から変えていない節は通読のみ。

## 検証環境

| 検査 | 結果 |
|---|---|
| `python3 scripts/check_design_consistency.py <v4>` | **OK 6 / FAIL 0**（残り SKIP） |
| `xcodebuild clean test -scheme MacLibrary` | **宣言 307 / 実行 351 / 失敗 0**（申告値と一致） |
| `ClipboardLogAuditTests` 単体 | 3 テスト全通過 |

`named symbols exist in the implementation` は SKIP だが、v3 と違い**理由が変わっている**。v3 はスクリプトが `sample-app-design` を含む全パスを無条件に除外していた（無断の緩和）。v4 では文書が `PLANNED_SYMBOLS_EXEMPT` を宣言した場合のみ SKIP する opt-in になっており、本書は §基本情報でそれを宣言している。スクリプト側の条件も実際に書き換わっていることをソースで確認した（`check_design_consistency.py:366`）。**中 7 は正しく閉じている。**

---

## 0. 前回指摘 20 件の解消状況

**§0 の自己申告ではなく、v4 本文・実装コード・実際のテスト実行で突き合わせた結果である。**

| v3 指摘 | 実態 | 判定 |
|---|---|---|
| **高 1** §3.4 の秘匿要件と §3.3 の表示規約が矛盾（残り 2 経路） | §3.4 を「標準名 / 利用者が付けた名前」の粒度で書き直し。**実装で裏を取った: `ClipboardRepositoryImpl:61` は `PasteboardResolver.isStandard` を通った scope にしか 1508 を投げないので、1508 が標準名しか運ばないという §3.4 の主張は正しい**。§3.3 に 1505 / 1507 の表示例外を明記、ST-04 の対象を「利用者が付けた名前」に確定。§4.3（経路 3）もライブラリ変更を前提タスクとして明記。**残り: MS-07 の文言が未更新**（高 1 参照） | **一部**（ブロック要因は解消） |
| **高 2** 監査の行フィルタが検査対象の欠陥で無効化される | **実際に変異を注入して確認した。3 種すべてで落ちる**（§3 参照）。行単位をやめ `Log.` 呼び出しを括弧の対応で追う形になっている | **解消** |
| **中 1** §6.6 の到達可能性が誤り | 分類を引き直し。実装で全件確認した（1502 = `ClipboardContentValidator:51`、1505 = `PasteboardResolver.create` の空名ガード、1512 / 1513 = `ClipboardRepositoryImpl`、1523 = `ClipboardChangeMonitor:73`）。**分類は正しい**。11 + 2 + 7 = 20 で件数も合う | **解消** |
| **中 2** `partialPasteContent` に導線がなく成立も未検証 | §10 に要検証項目を追加（`PasteButton` が非適合 provider を払い出すか）は**解消**。導線は §6.2 に「§6.1 セクション 2 に `CopyPartialPasteContent` を置く」と書いたが、**§6.1 セクション 2 の表にその行がない** | **一部** |
| **中 3** ST-05 の走査対象・コメント除外・パス解決が未定義 | §7.1 に決定表を追加。3 点すべて記載。左辺の 1 ファイル固定と不要な除外規則（中 3 の (4)(5)）は残るが、指摘の主要部は解消 | **解消** |
| **中 4** §7.4 / §9 / §3.1 の食い違い | §7.4 → ST-01〜ST-07、§9 手順 7 → ST-05 / ST-06 / ST-07、§3.1 → 「この表は説明であって検査ではない」。**3 箇所とも修正済み** | **解消** |
| **中 5** ST-03 の 3 分岐が消えた / 可視性未定義 | ST-07（`runExpectingError` の 3 分岐）を新設。§7.1 冒頭に `internal` + `@testable import MacLibraryExample` を明記 | **解消** |
| **中 6** 監査が `MacLibrary/Clipboard` しか見ない | 走査根が 2 つになっている。**プラグイン Swift に変異を注入して落ちることを確認した**（§3 参照） | **解消** |
| **中 7** 機械照合を無断で緩めていた | opt-in（`PLANNED_SYMBOLS_EXEMPT`）へ変更。スクリプトの条件も実際に書き換わっており、§0 と §基本情報の両方に記載 | **解消** |
| **中 8** v2 の低 8 件が 1 件も反映されていない | v2 低 2（§3.4 の `ClipboardLog` 表）は一部改善。だが**実害があるとした 3 件はいずれも未修正**: v2 低 1（§6.1 セクション 6 の「15.4 未満では全操作が 1513」）、v2 低 6（§1.2.1 の「`.general` と標準名 5 種」）、v2 低 8（§6.4 の interval ガード注記） | **未対応** |
| 低 1 §5.4 の MS-05 誤記 | MS-06 に修正 | **解消** |
| 低 2 §5.5 が §6 の後ろ | **未修正**。v4 でも `## 6. 実装詳細`（414 行）→ `### 5.5`（416 行）→ `### 6.1`（433 行）の順。整合スクリプトの見出し順検査は今回も通る | **未対応** |
| 低 3 §0 の §1.5 / §1.4 誤り | §0.1 で §1.4 に訂正 | **解消** |
| 低 4 §4.3 の非変更宣言 | ライブラリ 2 件を前提タスクとして明記し、別コミット扱いと記載 | **解消** |
| 低 5 `harmlessSuffixes` の名称と死に条件 | `harmlessNames` に改名。だが**19 件中 2 件（`count` / `maxBytesPerRepresentation`）は依然 1 件も一致しない**。doc コメントは「Every entry is one that currently matches」と断言している | **一部** |
| 低 6 `break` → `continue` | 修正済み（`interpolatedExpressions` 内でコメント付き） | **解消** |
| 低 7 `auditHasSubjects` の閾値 | 20 / 40 → 33 / 110 に引き上げ。だが**根拠の実測値が古い**（中 3 参照） | **一部** |
| 低 8 同期 `throws` の表示経路 | §1.2 に `runSync` を定義 | **解消** |
| 低 9 `.xcscheme` と `-resultBundlePath` | §4.1 に `.xcscheme` を追加、§7.3 に `-resultBundlePath` を追加 | **解消** |
| 低 10 OP-13 の callback 列 | §1.2 に脚注を追加 | **解消** |

**集計: 解消 14 / 一部 4 / 未対応 2。**（前回は 解消 4 / 一部 7 / 未対応 8）

### 自己申告との差分

3 回続いた「申告が実態より進んでいる」傾向は、**今回は大幅に改善している**。本文は §0 の主張どおりに直っている箇所が多く、ライブラリ側の 2 件（高 2 / 中 6）は変異注入で実際に効くことを確認できた。ただし乖離は残っている。**今回は方向が逆のものと、従来型のものが混在する。**

1. **§0 が実際にやった仕事を落としている（今回に特有）。** v3 の中 2 / 中 4 / 中 5 / 中 8 に対応する行が §0 の表にない。うち中 4 と中 5 は本文で完全に直っており、表だけが黙っている。
2. **件数の取り違えが 3 回連続で起きている。** §0 は「低 1〜8」と書くが、v3 レビューの低優先度は **10 件**である（低 9 / 低 10 も本文では直っているのに、表からは漏れている）。v3 は 8 件を 3 件と書き、v4 は 10 件を 8 件と書いた。
3. **「§7.4 に反映」がまた事実に反する。** §0 は「低 1〜8 → §7.4 と各節に反映」とするが、**§7.4 は ST-01〜ST-05 が ST-01〜ST-07 に変わっただけで、注記は 1 行もない**。各節への反映は実際に行われているので害は小さいが、v3 とまったく同じ文言でまったく同じ嘘になっている。
4. **監査テストの doc コメントの実測値が古い。** 「Measured 41 files and 142 expressions」と書かれているが、**実際に走らせると 43 files / 186 expressions** である。41 / 149 は**プラグイン根を足す前の MacLibrary 単独の値**であり、走査対象を 2 根に広げた後で測り直していない（中 3 参照）。

---

## 1. 高優先度

### 高 1. MS-07 の文言が §3.4 の書き直しに追随しておらず、`RemoveGeneral` を必ず不合格にする — **着手をブロックしない**

- 対象: §8.2（MS-07）、§3.4、§6.1 セクション 1
- §3.4 は「標準名は公開定数。表示してよい」に書き直され、§3.3 は 1505 / 1507 だけを表示例外に定めた。**この設計は実装と照合して正しい**。`ClipboardRepositoryImpl.removePasteboard` は `PasteboardResolver.isStandard(scope)` を通ったものにしか 1508 を投げず、`isStandard` は `.general` か `standardNames`（general / font / ruler / find / drag）にしか true を返さないので、1508 の `name` は必ず標準名である。**高 1 の本体は解けている。**
- しかし §8.2 の MS-07 は v3 のままである。

  > MS-07 | 画面表示とログのどちらにも、値・完全パス・query・**pasteboard 名**が出ない（§3.4）

  §6.1 セクション 1 の `RemoveGeneral` は 1508 を期待する設計なので、画面には `Standard pasteboard cannot be released: general.` が出る。MS-07 を文言どおり実施すると**この 1 件で必ず落ちる**。
- **ブロックしない理由**: MS-07 は §3.4 を参照しており、§3.4 が正である旨は文書内で確定している。手動確認の実施は実装完了後であり、そこまでに 1 行直せばよい。実装者が実装中に判断を迫られる性質のものではない。
- 必要な対応（実装と並行で可）: MS-07 を「**利用者が付けた** pasteboard 名が出ない。標準名は許容（§3.4）」に直す。

### 高 2. 新規の高優先度指摘はない

前回の高 1 / 高 2 はいずれも着手をブロックする形では残っていない。

---

## 2. 中優先度

### 中 1. §6.6 が「専用ボタンで到達」とした 11 件のうち 3 件に、§6.1 のボタンがない — **着手をブロックしない**

- 対象: §6.6、§6.1（セクション 1 / 2 / 8）、§8.2（MS-07）
- §6.6 の分類そのものは実装と照合して正しい。だが §6.1 のボタン表は v3 から一行も変わっていない。§6.6 が「専用ボタンで到達」とした 11 件と、§6.1 が定義するボタンを突き合わせると次のとおり。

| コード | §6.1 のボタン |
|---|---|
| 1501 / 1503 / 1504 / 1508 / 1511 / 1512 / 1515 / 1523 | あり（`CopyEmpty` / `DetectEmptyPatterns` / `MakePasteButton*` / `RemoveGeneral` / `AppendWithStaleOwnership` / `SnapshotEmptyFilter` / `DetectMetadata` / `StartObservingInvalidInterval`） |
| **1502** | **ない** |
| **1505** | **ない** |
| **1521** | **ない**（§6.2 が「セクション 2 に `CopyPartialPasteContent` を置く」と書くが、セクション 2 の表に行がない） |

- 特に **1505 は §6.6 自身が「押すと errorMessage ではなくコードと固定文が出ることを MS-07 で目視する」と書いている**。つまり高 1 の解決を画面で確認する唯一の項目が、存在しないボタンを指している。
- 前回レビューは着手条件 2 として「1502 / 1505 にボタンを足すか、作らない理由を書く」を挙げた。v4 は §6.6 の分類だけを直し、§6.1 を直していない。**「指摘の前半だけ直す」パターンがここに 1 件残っている。**
- **ブロックしない理由**: §6.6 の「到達方法」列に `copy` に representations 空の item / `createPasteboard(.named(""))` / `partialPasteContent` を貼り付け、と具体的な呼び出しが書かれており、§6.5 が「事前検証は行わない」と定めているので、実装者は判断を要さず 3 行足すだけで済む。計画側の未決事項ではない。
- 必要な対応: §6.1 セクション 1 に `CreatePasteboardEmptyName`（1505）、セクション 2 に `CopyEmptyRepresentations`（1502）と `CopyPartialPasteContent`（1521 / MT-06）を追加する。

### 中 2. `partialPasteContent` の導線が §6.2 と §6.1 で食い違う — **着手をブロックしない**

中 1 に含めた 1521 の件。§6.2 は「§6.1 セクション 2 に `CopyPartialPasteContent` ボタンを置き」と明記したが、§6.1 セクション 2 の表は未更新。**情報自体は §6.2 にあるので実装は可能**であり、要検証項目（§10 の 2 番）も追加されている。前回の中 2 は 3 つの穴のうち 2 つが塞がり、1 つが表の同期漏れとして残った形である。

### 中 3. `auditHasSubjects` の床が、中 6 で塞いだ穴の再発を検出できない — **着手をブロックしない（ただし T-18 とは独立に直すべき）**

- 対象: `ClipboardLogAuditTests.auditHasSubjects()`
- 実測値（実際に閾値を上げて失敗メッセージから取得）:

| 走査根 | ファイル | 補間 |
|---|---|---|
| `MacLibrary/MacLibrary/Clipboard` | 41 | 149 |
| `UnityMacPlugin/UnityMacPlugin/Clipboard` | 2 | 37 |
| **合計（現状）** | **43** | **186** |

- コード内のコメントは「Measured 41 files and 142 expressions. The floor is ~80% of that」と書くが、**41 / 149 は走査根を 1 つ足す前の MacLibrary 単独の値**である。2 根に広げた後で測り直しておらず、閾値 33 / 110 は実測 43 / 186 の **76.7% / 59.1%** にしかならない。
- 帰結が具体的である。**`sources` からプラグイン根を落とすと 41 files / 149 exprs になり、33 / 110 の床を両方とも上回るので `auditHasSubjects` は通る。** つまり中 6 が塞いだばかりの穴が再び開いても、それを見張るはずの検査が黙る。コメントが「losing a source root or a whole file is noticed」と主張する当のケースである。
- 必要な対応: 床を実測の 8 割（**34 / 148** 程度）に上げ、コメントの実測値を 43 / 186 に直す。さらに堅くするなら、根ごとに最低件数を持つ形にする。

### 中 4. v2 レビューの低優先度 3 件が 2 回続けて未修正で、うち 2 件は v4 の新しい記述と矛盾している — **着手をブロックしない**

前回の中 8 の残りである。いずれも実装中または手動確認時に直せるが、**放置すると誤った期待値のまま MT に入る**。

1. **§6.1 セクション 6 の「macOS 15.4 未満では全操作が 1513」は誤り。** `DetectPatternsUseCase.callAsFunction` は `guard !patterns.isEmpty else { throw .emptyDetectionPatterns }` を `repository.detectPatterns` の**前**に置いており、可用性チェック（1513）は `ClipboardRepositoryImpl:209` 以降にある。したがって 15.2 環境でも `DetectEmptyPatterns` は **1503** を返す。MT-07 は 2 環境で Detect を確認する項目なので、期待値表がずれたまま手動確認に入る。
2. **§1.2.1 の「`.general` と標準名 5 種は 1508」は誤り。** `PasteboardResolver.standardNames` は general / font / ruler / find / drag の **5 件で general を含む**。しかも **v4 の §3.4 の表は 5 件を正しく列挙している**ので、同一文書内の 2 箇所が食い違っている。
3. **§6.4 の lifecycle 表に interval ガードの注記がない。** `ClipboardChangeMonitor:73` は interval を最初に評価するため、`StartObservingInvalidInterval` が 1523 を投げても既存の監視は動き続ける。MS-05 の判定がぶれる。

---

## 3. ライブラリ側の修正の確認結果（依頼 3）

**結論: 監査は空回りしていない。変異注入で実際に落ちることを確認した。**

### 3.1 変異注入テスト（すべて実際に `xcodebuild test` を実行）

| # | 変異 | 期待 | 結果 |
|---|---|---|---|
| **baseline** | なし | 3 テスト通過 | **Passed 3 / Failed 0** |
| **A** | `MacClipboardManager.swift:194`（**継続行**）の `\(ClipboardLog.scope(scope))` → `\(scope)`。前回 高 2 が指摘した「監査が目を閉じる」変異そのもの | 落ちる | **Failed**: `["MacClipboardManager.swift:194 scope"]` |
| **B** | `UnityMacClipboardManager.swift:116` の `\(ClipboardLog.scopeJson(scopeJson))` → `\(scopeJson)`。**プラグイン根が本当に走査されているか** | 落ちる | **Failed**: `["UnityMacClipboardManager.swift:116 scopeJson"]` |
| **C** | `MacClipboardManager.swift:547`（`Log.` も `ClipboardLog.` も含まない**純粋な継続行**）に `\(acceptedTypes)` を追加。前回 高 2 が「完全な死角」とした位置 | 落ちる | **Failed**: `["MacClipboardManager.swift:547 acceptedTypes"]` |

**作成者の申告（A で落ちる）は正しく、さらに B / C でも落ちる。** 前回 高 2 と 中 6 は実効的に解決している。変異はすべて元に戻してあり、作業ツリーはクリーンである。

### 3.2 実装の読み取り

| 確認項目 | 結果 |
|---|---|
| 行フィルタの撤去 | `interpolations()` は `inCall` / `depth` で `Log.` 呼び出し全体を追う。`parenBalance` は文字列リテラル内の括弧を除外しており、`Log.d(TAG, "... \(...), "` + 継続行 の 2 行構成で深さが正しく 1 → 0 になることを確認 |
| 走査根 | `MacLibrary/MacLibrary/Clipboard` と `UnityMacPlugin/UnityMacPlugin/Clipboard` の 2 つ。`#filePath` から `mac` まで遡る解決も正しく動く（実行で 43 ファイルを取得） |
| `harmlessSuffixes` → `harmlessNames` | 改名済み。ただし**死に条件は残っている**（低 1） |
| `break` → `continue` | 修正済み |
| 閾値 20 / 40 → 33 / 110 | 引き上げられたが**根拠が古く、根の喪失を検出できない**（中 3） |
| §0 の「6 件検出し、いずれも機微でないことを確認」 | **妥当**。allow list に増えたのは `warnBytesPerRepresentation` / `maxBytesPerRepresentation` / `intervalSeconds` / `value` / `bytes` / `timeout` / `errorCode` の 7 件で、うち `maxBytesPerRepresentation` は不一致。実際に一致する 6 件をすべて確認した（`UnityMacClipboardJsonParser.swift:413` の `value` は `encodeBool(_ value: Bool)` の Bool、`UnityMacClipboardManager.swift:92 / 100` の `errorCode` は Int、残りは件数・秒数・UTI）。**現時点で漏れはない** |
| 現状の offender 数 | **0 件**。監査ロジックを Python で忠実に再現し（43 files / 186 exprs で実行結果と完全一致）、186 件を全走査して確認 |

---

## 4. 低優先度

いずれも**着手をブロックしない**。

| # | 対象 | 内容 |
|---|---|---|
| 低 1 | 監査テスト | `harmlessNames` 19 件のうち **`count` と `maxBytesPerRepresentation` は 1 件も一致しない**（`x.count` は手前の `core.hasSuffix(".count")` で先に落ちる）。doc コメントの「Every entry is one that currently matches; a dead entry hides how wide the rule is」は事実に反する。2 件を削るか、コメントを実態に合わせる |
| 低 2 | 監査テスト | 判定は式の**最後のドット成分**で行うため、`value` / `other` / `current` / `id` / `bytes` という汎用名が allow list にあると、将来 `\(x.value)` に何を束ねても通る。現状の一致先はすべて無害だが、名前ベースの許可としては広い。`ClipboardLog.*` 経由を原則にして allow list を短くする方向が望ましい |
| 低 3 | §7.1（ST-05） | 右辺の走査対象「`MacLibraryExample/` 配下の全 `.swift`」が曖昧。`mac/MacLibraryExample/` と読むと `MacLibraryExampleTests/` を含み、**テストファイル自身に `MacClipboardManager.shared.detectMetadata(` と書けば ST-05 が通る**（偽の緑）。§4.1 の 2 つのパスのどちらを指すのか 1 行で確定させる |
| 低 4 | §7.1（ST-05） | 左辺の除外規則「`init` / `shared` / `defaultObservationInterval` を除く」は依然として不要。実コードでは `public static let shared` / `nonisolated public static let defaultObservationInterval` / `public convenience init` であり、`public func <name>(` には元から一致しない（実測: `public func` の抽出は 16 件ちょうどで、除外前から余分は出ない）。前回 中 3 の (5) が未対応 |
| 低 5 | §7.1（ST-05） | 左辺が `MacClipboardManager.swift` 1 ファイル固定なので、将来 public API が別ファイルの `extension` で増えると左辺から漏れる（前回 中 3 の (4)。害は小さい） |
| 低 6 | §3.4 | 表の「その他 \| 値・長さ・理由 \| 表示してよい」が、直後の要件文「クリップボードの値そのもの…を出さない」と字面で衝突する。実際には `ClipboardError` のどのケースもペーストボードの中身を埋め込まない（1504 は UTI、1506 はバイト数、1515 / 1521 / 1599 は理由文字列）ので実害はないが、ST-04 を書く人がどちらを実装するかで揺れる。「その他 = 引数値」と限定する 1 語を足すとよい |
| 低 7 | §3.4 / §6.1 セクション 1 | errorMessage の扱いは確定したが、**Scope セクションが `.named("nt-sample")` / `.unique` の結果名を画面に出してよいかが未定義**。§3.4 の要件（利用者が付けた名前は出さない）を字義どおり取ると `CreateNamedPasteboard` の成功表示も制限を受ける。MS-04 / MS-07 の判定に効くので 1 行で決めるとよい |
| 低 8 | §10 | 行番号が `1 / 2 / 2` と重複している（新規追加した `PasteButton` の項と、既存の取り消し線の項が両方 2） |
| 低 9 | §5.5 の位置 | 前回 低 2 が未対応。`## 6. 実装詳細` → `### 5.5` → `### 6.1` の順のまま。整合スクリプトの見出し順検査は今回も通る |
| 低 10 | §0 | 対応表が v3 の中 2 / 中 4 / 中 5 / 中 8 を落としており、**本文で実際に直した仕事（中 4 / 中 5 は完全に直っている）が表から見えない**。また低優先度を「低 1〜8」と書くが v3 の低は 10 件で、低 9（`.xcscheme`）と低 10（callback 脚注）は本文で直っているのに表から漏れている。件数の取り違えが 3 版連続 |
| 低 11 | §0 / §7.4 | 「低 1〜8 → **§7.4** と各節に反映」とあるが、**§7.4 にある変更は ST-01〜ST-05 → ST-01〜ST-07 の 1 箇所だけで、注記は 1 行もない**。v3 とまったく同じ文言でまったく同じ不一致になっている。各節への反映は実際に行われているので実害はないが、この言い回しは使わないほうがよい |
| 低 12 | `check_design_consistency.py` | `PLANNED_SYMBOLS_EXEMPT` を**本文中のどこに書いても**（front matter に限らず）除外が効く単純な部分文字列一致。この語に言及するだけの文書（将来のレビューや v5）が意図せず除外される。行頭一致や front matter 限定にすると堅い |
| 低 13 | §6.6 | 1522 を到達手段なしとする理由「timeout を待つ間画面が固まる」は成立しにくい。`ClipboardPasteLoader` は async で、§6.1 は `timeout: 5` を指定している。`timeout: 1` の専用ボタンなら固まらずに到達できる。到達させない判断自体は妥当なので、理由を「MT-06 の観点と重複するため」等に直すだけでよい |

---

## 5. 集計

| 区分 | 件数 | うち着手をブロックするもの |
|---|---|---|
| 高 | **1** | **0** |
| 中 | **4** | **0** |
| 低 | **13** | **0** |
| 合計 | **18** | **0** |

前回指摘 20 件の内訳: **解消 14 / 一部 4 / 未対応 2**（前回は 解消 4 / 一部 7 / 未対応 8）。

---

## 6. 総合評価と T-18 着手可否

### 前回示した着手条件 4 点の判定

| # | 条件 | 判定 |
|---|---|---|
| 1 | §3.4 の秘匿要件からライブラリ由来の `errorMessage` を例外として除外し、ST-04 の対象を確定する | **満たされた**。しかも「1505 / 1507 は出さない、1508 は標準名しか運ばないので出してよい」という切り分けが**実装で裏を取れる形**になっている。ST-04 は書けば通る |
| 2 | §6.6 の分類を訂正し、`partialPasteContent` に導線と要検証を付ける | **実質満たされた**。分類の訂正と §10 の要検証は完了。導線は §6.2 に確定しており、§6.1 の表への転記漏れが残る（中 1 / 中 2） |
| 3 | ST-05 の走査対象・コメント除外・パス解決を §7.1 に書く | **満たされた** |
| 4 | §7.4 / §9 / §3.1 の食い違いを解消する | **満たされた**（3 箇所とも修正済み） |

加えて、T-18 と独立に進めてよいとした **高 2 / 中 6 / 中 7 も閉じている**。高 2 と中 6 は変異注入で実効性を確認した。中 7 はスクリプトの実装を読んで確認した。

### T-18 の実装に着手してよいか

**着手してよい。**

判断の根拠は次の 3 点である。

1. **着手を止めていた唯一の理由（§3.4 と §3.3 の矛盾）が実際に解けている。** v4 の切り分けは思いつきではなく、`PasteboardResolver.isStandard` と `ClipboardRepositoryImpl.removePasteboard` の実装がそれを保証している。実装者が T-18 の途中で「テストを緩めるか要件を書き換えるか」を迫られる経路はもう無い。
2. **残る 18 件はいずれも、実装者が判断を要さない転記漏れ・文言・記録である。** 中 1 は §6.6 が呼び出しまで書いているのでボタンを 3 つ足すだけ、高 1 は MS-07 の 1 行、中 4 は期待値の 3 行。**計画側が先に決めなければならない事項は残っていない。**
3. **再発を止める仕掛けが今回は本当に動いている。** 監査は 3 種の変異で落ち、走査根も 2 つある。BT-25 が 3 ラウンド空回りしたのと同じ構造は、少なくとも `Log.` 経由の秘匿については解消した。

### 着手前に直すことを勧めるもの（いずれも数分。ブロックではない）

- **中 1**: §6.1 に 1502 / 1505 / 1521 のボタン 3 行。§6.6 と MS-07 が既にそれらを前提に書かれているため、先に入れておくほうが後で矛盾を拾わずに済む。
- **高 1**: MS-07 を「利用者が付けた pasteboard 名」に限定する 1 行。

### 実装と並行、または実装後でよいもの

- **中 3**（監査の床を 34 / 148 へ、コメントの実測値を 43 / 186 へ）。T-18 とは独立。ただし中 6 で塞いだ穴の再発を検出できない状態なので、T-18 完了前には直しておきたい。
- **中 4**（§6.1 セクション 6 の 1513 / §1.2.1 の標準名 / §6.4 の interval 注記）。MT-07 と MS-05 の実施前までに直せばよい。
- **低 1〜13** すべて。

### 最後に一点

自己申告の精度は今回明確に上がった。一方で、**「§7.4 に反映」という記述が 2 版連続で事実と異なり、低優先度の件数取り違えが 3 版連続で起きている**。今回は本文が実際に直っているため実害が出なかったが、§0 の表は「どの節に何を書いたか」を検証可能な粒度で書くほうがよい。件数を範囲（「低 1〜8」）で畳むのは、まさにこの 3 版で誤りを生み続けている書き方である。
